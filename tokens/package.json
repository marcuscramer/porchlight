{
  "name": "porchlight-tokens",
  "private": true,
  "type": "module",
  "devDependencies": {
    "style-dictionary": "^5"
  },
  "scripts": {
    "build:android": "style-dictionary build --config config/android.config.mjs",
    "build:web": "style-dictionary build --config config/web.config.mjs",
    "build": "npm run build:android && npm run build:web"
  }
}
